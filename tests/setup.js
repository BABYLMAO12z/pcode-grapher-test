/* =========================================================================
 * PCODE Grapher · tests/setup.js — setup chung cho vitest
 * (File này bị THIẾU trong repo trước đây dù vite.config.js khai setupFiles
 *  → toàn bộ `npm test` chết ngay khâu load. Giữ file này trong git!)
 * ========================================================================= */
import '@testing-library/jest-dom/vitest';
